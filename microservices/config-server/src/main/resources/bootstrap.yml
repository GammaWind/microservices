spring:
  application:
    name: configserver

  cloud:
    config:
      server:
        git:
          uri: https://github.com/GammaWind/microservices/tree/master/microservices/config-server-repository
          clone-on-start: true

    fail-fast: true